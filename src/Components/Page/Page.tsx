import React from 'react';
import './Page.css'

type PageProps = {
}

const Page = ({ }: PageProps) => (
    <div className="Page">
        <div>This is the content of the page. It is only a test for now but will be created later.</div>
    </div>
);

export default Page;
